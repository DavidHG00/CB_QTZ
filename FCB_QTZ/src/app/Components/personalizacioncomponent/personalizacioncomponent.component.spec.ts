import { PersonalizacioncomponentComponent } from './personalizacioncomponent.component';

describe('PersonalizacioncomponentComponent', () => {
  let component: PersonalizacioncomponentComponent;

  beforeEach(() => {
    component = new PersonalizacioncomponentComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
